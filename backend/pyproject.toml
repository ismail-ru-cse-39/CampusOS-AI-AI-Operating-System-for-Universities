[project]
name = "campusos-backend"
version = "0.1.0"
description = "CampusOS AI — Backend API and Agent Orchestrator"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"
